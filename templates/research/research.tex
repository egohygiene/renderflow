%% research.tex — Pandoc composition for the shared Renderflow LaTeX library.
%% Document semantics stay here; presentation primitives live in templates/latex.
\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$,$if(papersize)$$papersize$$else$a4paper$endif$]{article}

%% Renderflow injects these roots from the active template directory. The
%% defaults also support direct pandoc use from the repository root.
$if(renderflow-style-root)$
\edef\RenderflowStyleRoot{\detokenize{$renderflow-style-root$}}
$else$
\def\RenderflowStyleRoot{templates/latex}
$endif$
$if(renderflow-font-root)$
\edef\RenderflowFontRoot{\detokenize{$renderflow-font-root$}}
$endif$

%% Profile-level design token overrides. These remain semantic values rather
%% than package implementation details.
$if(renderflow-color-accent)$
\def\RenderflowColorAccent{$renderflow-color-accent$}
$endif$
$if(renderflow-color-ink)$
\def\RenderflowColorInk{$renderflow-color-ink$}
$endif$
$if(renderflow-color-muted)$
\def\RenderflowColorMuted{$renderflow-color-muted$}
$endif$
$if(renderflow-color-rule)$
\def\RenderflowColorRule{$renderflow-color-rule$}
$endif$
$if(renderflow-color-surface)$
\def\RenderflowColorSurface{$renderflow-color-surface$}
$endif$
$if(renderflow-margin-top)$
\def\RenderflowMarginTop{$renderflow-margin-top$}
$endif$
$if(renderflow-margin-bottom)$
\def\RenderflowMarginBottom{$renderflow-margin-bottom$}
$endif$
$if(renderflow-margin-inner)$
\def\RenderflowMarginInner{$renderflow-margin-inner$}
$endif$
$if(renderflow-margin-outer)$
\def\RenderflowMarginOuter{$renderflow-margin-outer$}
$endif$
$if(renderflow-paragraph-skip)$
\def\RenderflowParagraphSkip{$renderflow-paragraph-skip$}
$endif$

%% Semantic font roles use deterministic Latin Modern defaults. A profile may
%% select another bundled family or local-first file without changing styles.
$if(mainfont)$
\def\RenderflowMainFont{$mainfont$}
$endif$
$if(sansfont)$
\def\RenderflowSansFont{$sansfont$}
$endif$
$if(monofont)$
\def\RenderflowMonoFont{$monofont$}
$endif$
$if(renderflow-main-font-file)$
\edef\RenderflowMainFontFile{\detokenize{$renderflow-main-font-file$}}
$endif$
$if(renderflow-sans-font-file)$
\edef\RenderflowSansFontFile{\detokenize{$renderflow-sans-font-file$}}
$endif$
$if(renderflow-mono-font-file)$
\edef\RenderflowMonoFontFile{\detokenize{$renderflow-mono-font-file$}}
$endif$

\input{\RenderflowStyleRoot/renderflow-core.sty}
\RequirePackage{amsmath,amssymb}

$if(highlighting-macros)$
$highlighting-macros$
\renewenvironment{Shaded}{\begin{RenderflowShaded}}{\end{RenderflowShaded}}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

$if(title)$
\renewcommand{\RenderflowDocumentTitle}{$title$}
\renewcommand{\RenderflowRunningTitle}{$title$}
$endif$
$if(subtitle)$
\renewcommand{\RenderflowDocumentSubtitle}{$subtitle$}
$endif$
$if(author)$
\renewcommand{\RenderflowDocumentAuthors}{$for(author)$$author$$sep$; $endfor$}
\renewcommand{\RenderflowRunningAuthor}{$for(author)$$author$$sep$; $endfor$}
$endif$
$if(date)$
\renewcommand{\RenderflowDocumentDate}{$date$}
$endif$

\hypersetup{
$if(title-meta)$
  pdftitle={$title-meta$},
$endif$
$if(author-meta)$
  pdfauthor={$author-meta$},
$endif$
$if(subject)$
  pdfsubject={$subject$},
$endif$
$if(keywords)$
  pdfkeywords={$for(keywords)$$keywords$$sep$, $endfor$},
$endif$
  pdfcreator={Renderflow via Pandoc and Tectonic}
}

$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$

$if(natbib)$
\RequirePackage[$natbiboptions$]{natbib}
\bibliographystyle{$if(biblio-style)$$biblio-style$$else$plainnat$endif$}
$endif$
$if(biblatex)$
\RequirePackage[$if(biblio-style)$style=$biblio-style$,$endif$$for(biblatexoptions)$$biblatexoptions$$sep$,$endfor$]{biblatex}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
$endif$

\begin{document}

$if(title)$
\RenderflowMakeTitle
$endif$

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
\vspace{1em}
$endif$

$for(include-before)$
$include-before$
$endfor$

$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
\tableofcontents
\newpage
$endif$

$body$

$if(natbib)$
$if(bibliography)$
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$
$endif$
$if(biblatex)$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$
$endif$

$for(include-after)$
$include-after$
$endfor$

\end{document}
